{# Exemplo para demonstrar otimizações #}

program teste_otimizacao;
var
  x, y, z, w, resultado, temp1, temp2 : integer;

begin
  x := 10;
  y := 20;
  z := 5 + 3;
  temp1 := x + y;
  temp2 := x + y;
  w := 100;
  resultado := temp1 + temp2
end.
